## add host to config

1. open `~/.ssh/config` using editor
2. add lines
   ```config
   Host <name>
        Hostname <host>
        User awe
        Compression yes
   ```
 3. use `ssh <name>` for create setup connection

## enable login only key
